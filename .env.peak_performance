# Peak Performance Configuration
# Hardware: 16-core AMD EPYC-Genoa, 32GB RAM
# Target: 12,000 orders/second maximum throughput
# Use: source .env.peak_performance before starting

# Application Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
ENVIRONMENT=production
LOG_LEVEL=warn  # Reduce logging overhead for performance

# Simulation Configuration - Background Load
SIMULATION_ENABLED=true
SIMULATION_ORDERS_PER_SEC=1000  # Background market activity
SIMULATION_WORKERS=16  # Match CPU cores
SIMULATION_SYMBOLS=BTCUSD,ETHUSD,ADAUSD,DOTUSD,SOLUSD,LINKUSD,AVAXUSD,MATICUSD
SIMULATION_VOLATILITY=0.08
SIMULATION_ENABLE_VOLATILITY=true

# Metrics Configuration
METRICS_ENABLED=true
METRICS_COLLECTION_TIME=60s
METRICS_RETENTION_TIME=24h
METRICS_EXPORT_INTERVAL=5m

# Database Configuration
DB_MAX_CONNECTIONS=100  # High concurrency support
DB_MAX_IDLE_TIME=30m
DB_MAX_LIFETIME=1h

# Health Check Configuration
HEALTH_ENABLED=true
HEALTH_CHECK_INTERVAL=30s
HEALTH_TIMEOUT=10s

# Server Timeouts
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s
SERVER_IDLE_TIMEOUT=120s

# CORS Configuration
ENABLE_CORS=true

# Go Runtime Configuration (set via export)
# export GOMAXPROCS=16
# export GOGC=100
# export GOMEMLIMIT=28GB
